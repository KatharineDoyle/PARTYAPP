<h1>Congratulations!</h1>
<h2>Here is your booking:</h2>

<%= @space.name %>
<%= @space.description %>
<p><%= @booking.date.to_date.strftime('%A, %b %d') %></p>

<%= link_to 'See my bookings', bookings_path %>
<%= link_to 'Search more party spaces', spaces_path %>
<%= link_to 'Add a review', new_space_booking_review_path(@booking) %>

<h4>Add a review!</h4>

