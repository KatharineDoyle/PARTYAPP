<p id="notice"><%= notice %></p>
<body>

<div class="flex-container">
  <div class="card-container">
   <div class="row">
    <div class ="col-xs-12 col-sm-6 col-md-4 col-lg-3 flex-item">
      <h2 class="text-center"><%= @space.name %></h2>
      <%= cl_image_tag @space.photo, height: 300, width: 400, crop: :fill %>
      <p><%= @space.description %></p>
      <p><%= @space.address %></p>
      <% if policy(@space).edit? %>
      <p> <%= link_to "Book your party", new_space_booking_path(@space), class: 'btn-blue' %></p>
      <p><%= link_to 'Back', spaces_path %></p>

<div class="l-flex-fix">
  <div class="l-flex-parent">
      <div class="space-info l-flex-child l-grid-half">
        <h2 class="text-center"><%= @space.name %></h2>
        <div class="example-item">
        <%= cl_image_tag @space.photo, height: 400, width: 800, crop: :fill %>
        <p><%= @space.description %></p>
        <p><%= @space.address %></p>
         <h4>Reviews</h4>
         <ul>
            <% @space.reviews.each do |review| %>
            <li>
            <p><%= review.content %></p>
            </li>
            <% end %>
          </ul>
        <h4>Add a review:</h4>
          <%= simple_form_for [@space, @review] do |f| %>
          <%= f.input :content%>
          <%= f.submit %>
          <% end %>
        <br><br>
        <% if policy(@space).edit? %>
        <% end %>
        <p> <%= link_to "Book your party", '#', class: "btn btn-primary" %></p>
        <% if policy(@space).edit? %>
        <% end %>
        <%= link_to 'Back', spaces_path %>
      </div>
      </div>


   <div class="map-cal l-grid-half">
   <div class="map-cal l-flex-child l-grid-half">
      <div class="example-item">

        <div
        id="map"
        style="width: 50vw;
        height: 100vh;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
  </div>
</div>
</div>

<% end %>

<<<<<<< HEAD

=======
>>>>>>> 810ca36c7d062eb6d56835e329526f5735237b12
